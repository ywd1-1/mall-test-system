# Copy this file to .env and fill every empty password before deployment.
MYSQL_ROOT_PASSWORD=
MYSQL_USER=mall_app
MYSQL_PASSWORD=
ADMIN_PASSWORD=

# These ports bind MySQL and the backend to the server itself, not the public network.
MYSQL_PORT=3306
BACKEND_PORT=8080
HTTP_PORT=80
